<?py
if not signkey:
?>
    echo "#{release_file},rpmattr=%config" | \\
<?py
#end if
?>
pmaker -n #{name}-release --license MIT \\
-w ${workdir} \\
--stepto sbuild \\
--group "Development/Tools" \\
--url #{baseurl} \\
--summary "Mock cfg files of yum repo #{name}" \\
--packager "#{.fullname}" \\
--email "#{email}" \\
--pversion #{distversion}  \\
--no-rpmdb --no-mock \\
--ignore-owner \\
--destdir #{workdir} \\
#{mock_cfg_file_list}

